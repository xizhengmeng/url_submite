# Submit Sitemap - URL批量提交工具

一个自动化的URL提交工具，可以从网站sitemap获取URL并批量提交到百度、Bing和Google搜索引擎。

## ✨ 特性

- 🌐 **多站点支持** - 同时管理多个网站的URL提交
- 🔄 **自动去重** - 自动记录已提交的URL，避免重复提交
- 📊 **配额管理** - 为每个平台单独配置每日提交配额
- 📦 **Sitemap解析** - 自动解析sitemap.xml（包括索引文件和压缩文件）
- 💾 **历史记录** - 本地保存提交历史，支持增量提交
- 📈 **实时进度条** - 直观显示sitemap解析和URL提交进度
- 📝 **详细日志** - 自动记录每次提交的详细信息到日志文件
- 🚀 **简单易用** - 每天只需运行一次命令

## 📋 支持的搜索引擎

| 搜索引擎 | API类型 | 说明 |
|---------|--------|------|
| 百度 | 百度站长平台API | 需要在百度站长平台获取token |
| Bing | IndexNow协议 | 快速、免费的实时索引协议 |
| Google | IndexNow协议 | 保留接口，待Google支持 |

## 🚀 快速开始

### 1. 构建程序

**方式一：使用构建脚本（推荐）**

```bash
./build.sh
```

自动构建所有 macOS 版本，包含彩色输出和详细信息。

**方式二：使用 Makefile**

```bash
# 构建所有版本
make build

# 快速构建当前平台版本
make quick

# 清理后重新构建
make clean && make build

# 查看所有命令
make help
```

**方式三：手动构建**

```bash
go build -o submit-sitemap ./cmd/submit-sitemap
```

**构建结果：**
- `submit-sitemap` - 当前平台通用版本
- `dist/submit-sitemap-darwin-arm64` - Apple Silicon 版本

### 2. 配置

复制配置示例文件：

```bash
cp config/config.example.yaml config/config.yaml
```

编辑 `config/config.yaml`，填入你的网站信息和API密钥：

```yaml
sites:
  - name: "我的网站"
    domain: example.com
    sitemap_url: "https://example.com/sitemap.xml"

    quotas:
      baidu: 100   # 每天提交100条到百度
      bing: 200    # 每天提交200条到Bing
      google: 150  # 每天提交150条到Google

    api:
      baidu:
        token: "your-baidu-token"
        site: "https://example.com"
      bing:
        api_key: "your-bing-api-key"
      google:
        api_key: "your-indexnow-key"
```

### 3. 运行

```bash
./submit-sitemap run
```

## 📖 使用说明

### 基本命令

```bash
# 运行提交任务（基本模式）
./submit-sitemap run

# 详细输出模式（同时显示日志到控制台）
./submit-sitemap run -v

# 查看提交统计
./submit-sitemap stats

# 测试配置文件
./submit-sitemap test

# 清除指定站点的历史记录
./submit-sitemap reset example.com

# 显示帮助
./submit-sitemap help
```

### 高级选项

```bash
# 使用自定义配置文件
./submit-sitemap run -c /path/to/config.yaml

# 详细输出模式 + 自定义配置
./submit-sitemap run -v -c /path/to/config.yaml
```

### 日志文件

所有运行记录都会自动保存到日志文件：

- **位置**: `data/logs/YYYY-MM-DD.log`
- **格式**: 带时间戳的详细日志
- **内容**: 包括配置加载、sitemap解析、URL过滤、提交结果等

示例日志内容：
```
2026/01/23 15:30:00 [INFO] ========== 提交任务开始 ==========
2026/01/23 15:30:00 [INFO] 配置加载完成 - 文件: config/config.yaml, 网站数: 2
2026/01/23 15:30:01 [INFO] Sitemap解析完成 - URL: https://example.com/sitemap.xml, 找到 1250 个URL
2026/01/23 15:30:01 [INFO] 开始提交 - 网站: example.com, 平台: baidu, URL数量: 100
2026/01/23 15:30:05 [SUCCESS] 提交完成 - 网站: example.com, 平台: baidu, 成功: 98
```

## 🔑 获取API密钥

### 百度站长平台

1. 访问 [百度站长平台](https://ziyuan.baidu.com/)
2. 添加并验证你的网站
3. 进入"普通收录" -> "API提交"
4. 复制接口调用地址中的token参数

### Bing IndexNow

1. 访问 [Bing Webmaster Tools](https://www.bing.com/webmasters)
2. 添加并验证你的网站
3. 生成IndexNow API密钥
4. 在网站根目录创建 `{api-key}.txt` 文件（内容为API密钥）

### Google (IndexNow)

目前Google尚未正式支持IndexNow协议，但保留了配置接口以便未来使用。

## 📁 目录结构

```
submit-sitemap/
├── cmd/
│   └── submit-sitemap/      # 主程序
├── internal/
│   ├── config/              # 配置管理
│   ├── sitemap/             # Sitemap解析器
│   ├── history/             # 历史记录管理
│   └── submitter/           # 搜索引擎提交器
├── pkg/
│   └── types/               # 公共类型定义
├── config/
│   ├── config.yaml          # 配置文件（需自行创建）
│   └── config.example.yaml  # 配置示例
├── data/                    # 数据目录
│   ├── submitted/           # 已提交URL记录
│   │   └── example.com/
│   │       ├── baidu.txt
│   │       ├── bing.txt
│   │       └── google.txt
│   └── logs/                # 日志文件
└── submit-sitemap           # 可执行文件
```

## ⚙️ 配置说明

### 网站配置

- `name`: 网站名称（可选，默认使用domain）
- `domain`: 网站域名
- `sitemap_url`: Sitemap URL（支持sitemap.xml、sitemap_index.xml、.gz压缩文件）

### 配额配置

- `baidu`: 每天提交到百度的URL数量
- `bing`: 每天提交到Bing的URL数量
- `google`: 每天提交到Google的URL数量

设置为0表示不提交到该平台。

### 全局设置

```yaml
settings:
  sitemap_cache_hours: 168  # Sitemap缓存时间（小时）
  timeout: 30               # 请求超时时间（秒）
  concurrent: 3             # 并发数
  log_level: info           # 日志级别
```

## 🔄 工作流程

```
1. 从sitemap获取所有URL
    ↓
2. 加载历史记录（已提交的URL）
    ↓
3. 过滤出未提交的URL
    ↓
4. 根据配额限制选择要提交的URL
    ↓
5. 提交到各个搜索引擎
    ↓
6. 保存成功提交的URL到历史记录
```

## 💡 最佳实践

### 定时任务

使用crontab设置每天自动运行：

```bash
# 编辑crontab
crontab -e

# 添加定时任务（每天早上9点运行）
0 9 * * * cd /path/to/submit-sitemap && ./submit-sitemap run >> data/logs/cron.log 2>&1
```

### 配额建议

- **百度**: 普通站点每日配额500条，新站3000条
- **Bing**: IndexNow没有严格限制，建议200-500条/天
- **Google**: 目前不支持，可设为0

### 多站点管理

可以在一个配置文件中管理多个网站：

```yaml
sites:
  - name: "主站"
    domain: example.com
    # ...

  - name: "博客"
    domain: blog.example.com
    # ...
```

## 🐛 故障排除

### Sitemap获取失败

- 检查sitemap URL是否正确
- 确认网站可以正常访问
- 检查sitemap文件格式是否正确

### 提交失败

- 验证API密钥是否正确
- 检查网站域名配置是否匹配
- 查看详细错误信息

### 历史记录问题

如需重新提交所有URL，可以清除历史记录：

```bash
./submit-sitemap reset example.com
```

## 📝 开发说明

### 依赖包

```bash
go get gopkg.in/yaml.v3
```

### 编译

**使用构建脚本（推荐）**

```bash
# 一键构建 macOS 所有版本
./build.sh
```

构建脚本会自动：
- 清理旧的构建文件
- 构建 Intel Mac 版本 (amd64)
- 构建 Apple Silicon 版本 (arm64)
- 构建当前平台通用版本
- 显示详细的构建信息

**手动编译**

```bash
# 当前平台
go build -o submit-sitemap ./cmd/submit-sitemap

# macOS Intel
GOOS=darwin GOARCH=amd64 go build -o submit-sitemap-darwin-amd64 ./cmd/submit-sitemap

# macOS Apple Silicon
GOOS=darwin GOARCH=arm64 go build -o submit-sitemap-darwin-arm64 ./cmd/submit-sitemap

# Linux
GOOS=linux GOARCH=amd64 go build -o submit-sitemap-linux ./cmd/submit-sitemap

# Windows
GOOS=windows GOARCH=amd64 go build -o submit-sitemap.exe ./cmd/submit-sitemap
```

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交Issue和Pull Request！

## ⚠️ 注意事项

1. **API密钥安全**: 不要将包含API密钥的配置文件提交到公开仓库
2. **配额限制**: 遵守各平台的配额限制，避免过度提交
3. **Sitemap更新**: 建议每周更新一次sitemap，配置的缓存时间为7天
4. **失败URL**: 提交失败的URL不会重试，下次运行时会再次尝试提交
